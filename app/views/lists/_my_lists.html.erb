<div class='row my-3'>
  <% @lists.each do |list| %>
    <a id='list' class="tablinks shadow-sm listTrigger border rounded bg-white col-3 mr-2 text-left p-3" data-target="<%= list.name%>" >
        <h5 class='text-primary'><%= list.name %></h5>
        <div class="d-flex justify-content-between">
          <p class='text-warning'><em><%= list.shop.address %></em></p>
          <div class='text-success'>
            <% if list.private %>
              <i class="fas fa-eye-slash"></i>
            <% else %>
              <i class="fas fa-eye"></i>
            <% end %>
          </div>
        </div>
    </a>
  <% end %>
</div>


<%# <div class="table-responsive">
  <table class="table table-striped table-sm">
    <thead>
      <tr>
        <th></th>
        <th>Nom</th>
        <th>Boutique</th>
        <th>Visibilité</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @lists.each do |list| %>
        <%# <tr>
          <td class="text-center">
            <a class="tablinks listTrigger" data-target="<%= list.name"> %>
              <%# <i class="btn-sm btn-outline-success fas fa-plus"></i>
            </a>
          </td>
          <td><%= list.name</td> %>
          <%# <td><%= Shop.find(list.shop_id).address</td> %>
          <%# <td><%= list.private ? "private" : "public"</td> %>
          <%# <td> %>
            <%# <%= link_to list_path(list), method: :delete, remote: true do %>
              <%# <i class="mr-2 fas fa-trash-alt"></i> %>
            <%# <% end %>
          <%# </td>
        </tr> %>
      <%# <% end %>
    <%# </tbody>
  </table>
</div> %>
